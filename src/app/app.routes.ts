import { Routes } from '@angular/router';
import { FormInsightsComponent } from './components';

export const routes: Routes = [
    {
        path: '',
        component: FormInsightsComponent
    }
];
